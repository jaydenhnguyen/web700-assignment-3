<!DOCTYPE html>
<html>

<head>
    <title><%= setDetail.name %> | Lego Collection</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/styles/theme.css" />

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch('https://dummyjson.com/quotes/random')
                .then(response => response.json())
                .then(data => {
                    document.getElementById("quote").innerHTML = `"${data.quote}" - ${data.author}`;
                })
                .catch(error => {
                    console.error('Error fetching quote:', error);
                });
        });
    </script>

</head>

<body>
<%- include('partials/navbar', {currentPage: 'sets'}) %>

<div class="container mt-4">
    <section class="bg-light p-4 mb-4 rounded text-gray">
        <h1><%= setDetail.name %></h1>
        <p class="lead">Below, you will find detailed information about the set: <%= setDetail.name %> (<%= setDetail.set_num %>)</p>
    </section>

    <div class="row">
        <div class="col-md-5">
            <img src="<%= setDetail.img_url %>" alt="<%= setDetail.name %>" class="img-fluid rounded mb-3">
        </div>
        <div class="col-md-7">
            <div class="card">
                <div class="card-header">
                    <h2>Set Details</h2>
                </div>
                <div class="card-body">
                    <table class="table">
                        <tr>
                            <th>Set Number:</th>
                            <td><%= setDetail.set_num %></td>
                        </tr>
                        <tr>
                            <th>Name:</th>
                            <td><%= setDetail.name %></td>
                        </tr>
                        <tr>
                            <th>Year:</th>
                            <td><%= setDetail.year %></td>
                        </tr>
                        <tr>
                            <th>Theme:</th>
                            <td>
                                <a href="/lego/sets?theme=<%= setDetail.Theme.name.toLowerCase() %>" class="btn btn-sm btn-outline-primary">
                                    <%= setDetail.Theme.name %>
                                </a>
                            </td>
                        </tr>
                        <tr>
                            <th>Number of Parts:</th>
                            <td><%= setDetail.num_parts %></td>
                        </tr>
                    </table>

                    <span id="quote"></span>

                    <div class="d-flex gap-2 mt-4">
                        <a href="#" onclick="history.back(); return false;" class="btn btn-secondary">Back</a>
                        <a href="/lego/deleteSet/<%= setDetail.set_num %>" class="btn btn-danger">Delete</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>